[package]
name = "aegis-intel"
version = "0.1.0"
edition = "2021"

[dependencies]
aegis-core = { path = "../core" }
aegis-events = { path = "../events" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
csv = "1.3"
tracing = "0.1"
