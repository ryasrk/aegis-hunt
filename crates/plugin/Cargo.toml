[package]
name = "aegis-plugin"
version = "0.1.0"
edition = "2021"

[dependencies]
aegis-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
chrono = { version = "0.4", features = ["serde"] }
